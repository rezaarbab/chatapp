{
  "name": "chatapp-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.22.0",
    "vite": "^7.3.6",
    "vitest": "4.1.0"
  }
}
